{
  "name": "boop-runner",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "BoopPr runner: clones a PR, runs the multi-lens review via OpenCode, and posts a friendly summary with inline comments",
  "scripts": {
    "start": "node src/index.mjs",
    "test": "bun test src/*.test.mjs src/lib/*.test.mjs",
    "test:node": "node --test src/*.test.mjs src/lib/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@octokit/rest": "22.0.1",
    "@openrouter/agent": "0.8.0",
    "jsonwebtoken": "9.0.3"
  },
  "//": "allowScripts whitelists @openrouter/sdk's postinstall (a non-blocking type-staleness check). The agent SDK pulls the client SDK as a transitive dep, so this entry is required for npm's allowScripts gate. The check exits 0 even when types are stale, so it is safe to allow.",
  "onlyBuiltDependencies": [
    "@openrouter/sdk"
  ]
}
